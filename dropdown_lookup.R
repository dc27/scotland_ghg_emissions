dropdown_lookup <- list(
  "pollutant" = "pickerInput",
  "year" = "sliderInput",
  # vehicle type for road transport
  "vehicle_type" = "selectInput",
  "road_type" = "selectInput",
  # body type for ULEVs
  "body_type" = "multiSelectInput",
  "statistic" = "selectInput"
)